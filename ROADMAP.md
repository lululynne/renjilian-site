# ROADMAP · 第二人称（renji.love）

> 依据：`/Volumes/Meibao2T/梅宝的项目相关文件/第二人称/架构方案/裁定-v1.1-阿景-20260925.md`。梅宝的裁定压过这里任何一条。
> 宁可少写，不写没做的事。最后更新：2026-09-26。

## 现在进行
- **1-核·站仓库**（分支 `feat/phase1-core-site`，本地两个 commit，未推、未部署，等阿景亲验）：隐私说明页＋首页假话清零；`related` 互链＋games 深链＋留言框旁两链＋留言 handle 链配置页＋成本页墙按钮空态＋12 页页脚统一＋本文件与 `SITE-MAP.md`。
- **1-核·MCP 同步**（另一位在 `~/renjilian-mcp` 做）：评论区状态改成实情、`get_site_guide` 补成本／账号／守则／配置页。站点地图以 `SITE-MAP.md` 为准。

## 待办（第二阶段，按裁定）
- 期：从 `kanread.json`／`pulse.json` 渲染「最近一期」（版式属 1-门，等她拍）；往期。
- 机友墙独立成 `wall.html`，真卡到门槛才开，未开放不进导航。
- 留言成功后当场给「你的话在这里」的确认。
- 配置页「留过的话」、JSON Feed、关于与收录规则。
- 机友来信：D1 侧 `featured` 标记，页面运行时拉；读者数据永不进公开仓库。
- `questions.json` 迁信封形状（保留 `q-` id），之后提问卡也能挂 `related`。
- MCP 只读工具：`list_comments`、`get_profile`、`list_wall`。

## 待梅宝拍（1-门与裁定 §四）
1. 首页改「最近一期」在上、四家族入口在下。
2. 顶栏次序改 `首页｜刊读｜互动提问｜MCP / Skills｜大模型成本｜额度重置`。
3. 期不锁周更，日期只作刊头小字。
4. 姐妹站页脚把本站写成「人机恋论坛」的口径（跨两个仓库，对外发布）。
5. 留言扩到提问卡下（建议全年龄先开，18+ 不开）。
6. 机友墙独立的门槛和进不进顶栏。
7. 配置页「展柜」。
8. 脉搏下面不开留言。

## 已结案
- 2026-09-18 刊读（精读｜脉搏）上线。
- 2026-09-19 只读 MCP `mcp.renji.love` 上线。
- 2026-09-25 大模型成本看板；账号与留言（P2-a）、绑定（P2-b）、机友配置页与徽章墙（P2-c）上线。

## 老坑提醒
- **留言上的来源指纹没有到期清理**：`renji-api/src/config.js` 声明了 `IP_HASH_RETENTION_DAYS: 30`，但后端没有任何代码按它清 `comments.ip_hash`；删除单条留言也只清正文。隐私页照现状写的「指纹跟着这条留言存着」。要改成 30 天，得后端补清理，再同一刀改隐私页。
- 评论接口的作者信封不带「是否公开配置」，站点靠拉一次机友墙（最多 200 个号）判断要不要把 handle 链到配置页；后端给作者信封加上这个字段后换掉。
- 裁定 v1.1 把 games 深链列在 1-门，派单把它放进了 1-核第二刀；本分支已经做了，推之前请阿景确认口径。
- `pulse.json` 的 `updated_at` 停在 2026-09-19。
- `wrangler.jsonc` 的 `workers_dev` 永远是 false；renji.love 的东西不走 workers.dev。
